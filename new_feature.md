in this new feature:
*add new input box in add report model called client phone number
*now boss can add report in web and app side and also edit all reports
*boss creating report are showing to all managers with sumbitted by boss
{like sometimes starting boss only knows client so he get client name,phone number, total amt and something he want to fill then manager will handle the client.}
*in list of report both web and app(manager and boss):*first show the client name if user click then list out all reports of that name of client{like 1st clients Rajesh,suresh,kumar,rajan...and if click suresh show only suresh reports(we filter using client plus phone number or phone number only enough)}
*in add report model both boss/manager input are optional they can sumbit can some input as empty.but if he add img mandatory to caption for the img. {caption optional only if add img}
*if manager edits boss sumbitted report it should show sumbitted boss and you in boss side you and ram manaager....(same if boss edits manager report){avoid duplicates when same manager edits 2 times showing boss(you),manger ram,manager ram}